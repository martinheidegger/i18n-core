JavaScript: true
engines:
  eslint:
    enabled: true
    config:
      config: .eslintrc
    checks:
      complexity:
        enabled: false
  nodesecurity:
    enabled: true
  markdownlint:
    enabled: true

ratings:
   paths:
   - "**.js"
   - "**.md"

exclude_paths:
- "CHANGELOG.md"
